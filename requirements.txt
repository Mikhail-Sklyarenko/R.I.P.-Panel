customtkinter>=5.2.0
cryptography>=42.0
pydantic>=2.0
pyyaml>=6.0
pillow>=10.0
pywin32; sys_platform == 'win32'
pydirectinput>=1.0.4; sys_platform == 'win32'
pytest>=8.0
# Optional Windows OCR for drop_picker live: pip install pytesseract (+ Tesseract binary)
